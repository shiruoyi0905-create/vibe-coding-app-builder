# Vibe Coding：从想法到可演示 App 的闭环 SOP

> 适用于个人或小团队借助 AI，在数小时到数天内完成 Web MVP。目标是得到**可运行、可验证、可继续迭代**的产品，而不是一次性 Demo。原生能力、支付、敏感数据、高并发或强合规项目需要额外设计。

## 先定义“完成”

开工前写下本轮完成标准：目标用户能独立走通核心任务；P0 无阻断错误，关键异常有恢复路径；项目可按 README 从零启动；手机和桌面端各走查一次；假数据、临时方案和后续事项已明确。

每阶段遵循：**输入 → 产出 → 验收 → 决策**。验收未通过就继续当前阶段，不用“先做完再说”掩盖问题。

## Phase 0 · 定义问题与 MVP（20–40 分钟）

**产出：** 一页产品简报、风险假设、P0/P1/P2 范围。

1. 写清目标用户、触发场景、现有替代方案、核心痛点、期望结果、差异化。
2. 将假设分为：用户是否需要、是否会用、技术是否可行、是否可持续。
3. 让 AI 反驳 idea，找出最可能使项目失败的 3 个假设及最低成本验证方式。
4. 用一条核心路径约束 MVP：`进入 → 核心动作 → 获得结果 → 保存/分享（如必要）`。
5. P0 仅保留核心路径不可缺少的功能；其余放入 P1/P2 停车场。

**阶段门：** 能用一句话说清“谁在什么场景下，用它完成什么”，P0 最好不超过 3–5 项。最大风险若是“没人需要”，先访谈或做落地页测试。

## Phase 1 · 体验原型（30–90 分钟）

**产出：** 可点击原型、关键状态清单、初步视觉方向。

1. 先制作一条端到端路径。可用单文件 React/HTML 或设计工具，QoderWork 只是可选预览环境。
2. 除理想状态，还要表现 loading、空、失败、重试和完成反馈。
3. 动效只服务状态变化、空间关系或情绪记忆点，避免为“高级感”堆动画。
4. 找 1–3 位接近目标用户的人完成任务，只给目标，不解释操作；记录卡住点和原话。
5. 每轮只解决最影响任务完成率的 1–2 个问题。

**阶段门：** 用户无需讲解即可走通核心路径；反馈明确；原型验证了高风险交互，而不只是看起来漂亮。

## Phase 2 · 固化产品规格（30–60 分钟）

**产出：** 轻量 PRD、验收标准、非目标清单。

PRD 至少包含：问题与目标、用户与场景、核心路径、P0 功能、页面/状态、数据与权限、非功能要求、成功指标、非目标。每个 P0 写可测试的验收标准：

```text
Given 用户未填写必填项
When 点击提交
Then 不发起请求，并在对应字段附近显示可操作的错误提示
```

维护简短决策日志：日期、决定、理由、被否决方案，防止后续对话推翻已确认选择。

**阶段门：** 只看 PRD 和原型即可判断实现是否合格；“高级、丝滑、智能”等词已转成可观察行为。

## Phase 3 · 技术方案与任务切片（30–60 分钟）

**产出：** 技术方案、数据模型、任务清单、验证计划。

1. 先检查现有仓库、运行环境和约束，再选技术栈，不默认重建项目。
2. 小型 Web MVP 可选 React + Vite；确需 SSR、服务端路由或全栈能力时再考虑 Next.js。
3. 明确组件边界、状态归属、路由、数据模型、接口契约、持久化、错误处理和环境变量。
4. 先做“垂直切片”：用尽量真实的链路跑通核心任务，再横向补页面。
5. 任务标注依赖和验证方式，确保每项能独立实现、预览和回退。

**安全底线：** 密钥只放环境变量且不提交 `.env`；认证、支付、上传、位置、健康或个人信息需单独做安全与隐私设计；外部服务优先使用沙箱。

**阶段门：** 复杂度与 MVP 匹配；最高风险有验证方案；任务没有被简单切成“所有前端/所有后端”。

## Phase 4 · 可验证地开发（1 小时起）

**产出：** 可运行项目、测试、启动说明。

1. 确认现有项目能启动，建立版本控制检查点，记录运行命令。
2. 按垂直切片实现：骨架 → 核心路径 → 真实数据/接口 → 异常状态 → 体验增强。
3. 每个切片执行“实现 → 静态检查 → 测试 → 浏览器走查 → 检查点”。
4. 要求 AI 报告改动文件、验证命令与结果、遗留假设，不接受“应该可以”。
5. 失败时先读完整报错并定位根因；连续两次同类修补无效，停止试错，回查复现、接口、版本和前置假设。

常见命令（以项目实际脚本为准）：

```bash
npm install
npm run dev
npm run lint
npm test
npm run build
```

**阶段门：** 新环境可按说明启动；核心路径可用；lint/typecheck/build 通过；关键逻辑和失败路径有自动化测试或明确人工记录。

## Phase 5 · 体验与质量打磨（持续）

逐屏记录“步骤、预期、实际、证据、优先级”。反馈使用：

```text
在[设备/页面]，当我[操作]时，实际[现象]；期望[可观察结果]。
验收方式：[具体步骤或截图参考]。
```

检查核心路径、刷新/返回/重复提交、断网与慢网；手机尺寸、键盘遮挡和触控区域；键盘操作、焦点、对比度、减少动态效果；loading/空/错/成功状态；性能、控制台和资源错误。先修任务阻断，再修理解成本，最后做装饰增强。音效与动效应可关闭或降级。

## Phase 6 · 发布与交付

发布前确认：生产构建通过；环境变量正确；无密钥或调试信息泄漏；404/错误页可用；埋点不过度收集；依赖和素材许可明确；生产环境复测核心路径；有回滚方式。

README 至少写产品简介、技术栈、安装/启动/测试/构建命令、环境变量示例、目录说明和已知限制。保留版本号、日期、变更、验证结果和回滚点。

## 与 AI 协作的上下文包

长任务不要依赖聊天记忆，每轮提供：

```text
目标：本轮要达成什么
基准：PRD/原型/技术方案的具体章节
现状：已完成与可复现结果
任务：只做哪些改动
约束：不得改变什么
验收：命令、操作路径和预期结果
交付：改动文件、验证结果、遗留风险
```

新需求先判断是缺陷、P0 变更还是 P1/P2；若改变核心路径，先更新 PRD 与验收标准。

## Prompt 模板

```text
# Idea 反驳
这是我的产品想法：[内容]。请识别目标用户、替代方案和最危险的 3 个假设，给出最低成本验证方式，并将功能分为 P0/P1/P2。

# 原型
基于这条核心路径制作可交互原型：[路径]。覆盖正常、加载、空、失败和完成状态。先确保任务可完成，再增加一个服务于体验的记忆点。

# PRD
根据已确认原型生成轻量 PRD。每个 P0 功能包含 Given/When/Then 验收标准，并明确非目标、数据/权限、成功指标和未决问题。

# 实现
先检查现有项目与文档，再实现：[任务]。不得改动：[约束]。完成后运行相关检查并报告命令、结果、改动文件和遗留风险。

# 修复
复现步骤：[步骤]；预期：[预期]；实际：[实际]；证据：[报错/截图]。先定位根因，再做最小修复，并补充防回归验证。
```

## 关键原则

| 原则 | 判断标准 |
|---|---|
| 问题先于功能 | 先验证需求，再增加代码 |
| 垂直切片 | 每轮产生可运行、可验证的用户价值 |
| 文档是共同基准 | PRD、技术方案和决策日志随变更更新 |
| 证据胜过感觉 | 用复现、测试、截图和用户观察判断 |
| 小步且可回退 | 一次解决少量问题，保留稳定检查点 |
| 先 Web 后原生 | 仅在原生能力确有价值时增加复杂度 |
| Demo 不等于上线 | 发布前补齐安全、隐私、可靠性与文档 |

## 时间预期

“1.5–2.5 小时”只适合范围极小、使用假数据的可演示原型。带真实接口、认证、持久化、部署和质量保障的 MVP 通常需要数小时到数天。以阶段门判断进度，不用时间承诺替代质量判断。
